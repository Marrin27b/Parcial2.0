#include <iostream>

using namespace std;

int main(){

    int temperatura[7];
    for(int i = 0; i < 7; i++){
        cout << "Ingrese la temperatura: " << i + 1 << " : " << endl;

        cin >> temperatura[i];
    }

    int max = 35
    int max_dias;
    for (int j = 0; j < temperatura; j++)
    {
        if (temperatura[j] > max){
            max = temperatura[j]
        }
        
    }
    